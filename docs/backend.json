
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["landlord", "agent", "tenant"] },
        "idleTimeoutMinutes": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "email", "role"]
    },
    "Property": {
      "title": "Property",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "landlordId": { "type": "string" },
        "tenantId": { "type": "string" },
        "tenantEmail": { "type": "string" },
        "activeTenantUids": { "type": "array", "items": { "type": "string" } },
        "address": { "type": "object" },
        "status": { "type": "string", "enum": ["Vacant", "Occupied", "Under Maintenance"] },
        "imageUrl": { "type": "string" },
        "additionalImageUrls": { "type": "array", "items": { "type": "string" } }
      }
    },
    "Tenancy": {
      "title": "Tenancy",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "landlordId": { "type": "string" },
        "propertyId": { "type": "string" },
        "userId": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "verified": { "type": "boolean" },
        "name": { "type": "string" },
        "telephone": { "type": "string" },
        "status": { "type": "string" }
      }
    },
    "Message": {
      "title": "Message",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "tenantId": { "type": "string" },
        "landlordId": { "type": "string" },
        "senderId": { "type": "string" },
        "content": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "google"]
  },
  "firestore": {
    "/users/{userId}": { "schema": { "$ref": "#/backend/entities/User" }, "description": "Profiles" },
    "/properties/{propertyId}": { "schema": { "$ref": "#/backend/entities/Property" }, "description": "Asset registry" },
    "/tenants/{tenantId}": { "schema": { "$ref": "#/backend/entities/Tenancy" }, "description": "Resident registry" },
    "/messages/{messageId}": { "schema": { "$ref": "#/backend/entities/Message" }, "description": "Private chat history" },
    "/repairs/{repairId}": { "description": "Maintenance requests" },
    "/expenses/{expenseId}": { "description": "Financial ledger" },
    "/rentPayments/{paymentId}": { "description": "Rent tracking" },
    "/documents/{documentId}": { "description": "Compliance vault" },
    "/contractors/{contractorId}": { "description": "Tradespeople directory" },
    "/inspections/{inspectionId}": { "description": "Safety reports" },
    "/screenings/{screeningId}": { "description": "Vetting reports" }
  }
}
