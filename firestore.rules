
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * RentSafeUK Security Governance v15.0
     * Optimized for high-speed query compatibility and atomic resident handshakes.
     */

    function isSignedIn() {
      return request.auth != null;
    }

    function getEmail() {
      return request.auth.token.get('email', '').lower();
    }

    function getUid() {
      return request.auth.uid;
    }

    // Helper to check if the user is the landlord of the record
    function isLandlord(data) {
      return isSignedIn() && getUid() == data.get('landlordId', '');
    }

    // Helper to check if the user is the tenant of the record (Handshaked UID)
    function isTenant(data) {
      return isSignedIn() && (
        getUid() == data.get('tenantId', '') || 
        getUid() == data.get('userId', '')
      );
    }

    // --- Identity Registry ---
    match /users/{userId} {
      allow read, write: if isSignedIn() && getUid() == userId;
    }

    // --- Tenant Registry ---
    match /tenants/{tenantId} {
      // DISCOVERY: Explicitly authorized for query-based scans matching identity keys
      allow list: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('userId', '') ||
        getEmail() == resource.data.get('email', '').lower()
      );
      
      allow get: if isLandlord(resource.data) || isTenant(resource.data) || getEmail() == resource.data.get('email', '').lower();
      allow create: if isSignedIn();
      
      // HANDSHAKE: Allow residents to link their UID if they match the registry email
      allow update: if isSignedIn() && (
        isLandlord(resource.data) || 
        (getEmail() == resource.data.get('email', '').lower() && request.resource.data.diff(resource.data).affectedKeys().hasAny(['userId', 'verified', 'joinedDate', 'status', 'lastSyncCheck']))
      );
      allow delete: if isLandlord(resource.data);
    }

    // --- Property Portfolio ---
    match /properties/{propertyId} {
      // DISCOVERY: Optimized for collection scans
      allow list: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '') ||
        resource.data.get('activeTenantUids', []).hasAny([getUid()]) ||
        getEmail() == resource.data.get('tenantEmail', '').lower()
      );

      allow get: if isLandlord(resource.data) || isTenant(resource.data) || getEmail() == resource.data.get('tenantEmail', '').lower();
      allow create: if isSignedIn();
      
      // HANDSHAKE: Allow residents to link their UID to the property bridge
      allow update: if isSignedIn() && (
        isLandlord(resource.data) || 
        (getEmail() == resource.data.get('tenantEmail', '').lower() && request.resource.data.diff(resource.data).affectedKeys().hasAny(['tenantId', 'activeTenantUids', 'status']))
      );
      allow delete: if isLandlord(resource.data);
    }

    // --- Secure Messaging Ledger ---
    match /messages/{id} {
      // Optimized for high-speed query compatibility (matches where filters)
      allow list: if isSignedIn() && (
        getUid() == resource.data.get('tenantId', '') || 
        getUid() == resource.data.get('landlordId', '')
      );
      allow get: if isSignedIn() && (
        getUid() == resource.data.get('tenantId', '') || 
        getUid() == resource.data.get('landlordId', '')
      );
      allow create: if isSignedIn() && (
        request.resource.data.get('senderId', '') == getUid() &&
        (getUid() == request.resource.data.get('tenantId', '') || getUid() == request.resource.data.get('landlordId', ''))
      );
      allow update, delete: if false;
    }

    // --- Shared Ledger Collections ---
    
    match /repairs/{id} {
      allow list: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '') ||
        getUid() == resource.data.get('reportedBy', '')
      );
      allow get: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '') ||
        getUid() == resource.data.get('reportedBy', '')
      );
      allow create: if isSignedIn();
      allow update: if isLandlord(resource.data) || isTenant(resource.data);
      allow delete: if isLandlord(resource.data);
    }

    match /documents/{id} {
      allow list: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '') ||
        getUid() == resource.data.get('propertyId', '')
      );
      allow read: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '') ||
        getUid() == resource.data.get('propertyId', '')
      );
      allow write: if isLandlord(resource.data);
    }

    match /inspections/{id} {
      allow list: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '')
      );
      allow read: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '')
      );
      allow write: if isLandlord(resource.data);
    }

    match /rentPayments/{id} {
      allow list: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '')
      );
      allow get: if isSignedIn() && (
        getUid() == resource.data.get('landlordId', '') ||
        getUid() == resource.data.get('tenantId', '')
      );
      allow write: if isLandlord(resource.data);
    }

    match /contractors/{id} {
      allow list: if isSignedIn();
      allow read, write: if isSignedIn();
    }

    match /expenses/{id} {
      allow list: if isSignedIn() && getUid() == resource.data.get('landlordId', '');
      allow read, write: if isSignedIn() && getUid() == resource.data.get('landlordId', '');
    }
    
    match /screenings/{id} {
      allow list: if isSignedIn() && getUid() == resource.data.get('landlordId', '');
      allow read, write: if isSignedIn() && getUid() == resource.data.get('landlordId', '');
    }

    match /checklists/{id} {
      allow list: if isSignedIn() && getUid() == resource.data.get('landlordId', '');
      allow read, write: if isSignedIn() && getUid() == resource.data.get('landlordId', '');
    }
  }
}
