rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Helper function to check ownership on a document
    function isOwner(doc) {
      return isSignedIn() && doc.ownerId == request.auth.uid;
    }

    // A user can manage their own profile document
    match /users/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }

    // A user can manage properties they own
    match /properties/{propertyId} {
      allow get: if isOwner(resource.data);
      allow list: if isSignedIn(); // Client-side queries always filter by ownerId
      allow create: if isOwner(request.resource.data);
      allow update, delete: if isOwner(resource.data);
    }

    // A user can manage tenants they own
    match /tenants/{tenantId} {
      allow get: if isOwner(resource.data);
      allow list: if isSignedIn(); // Client-side queries always filter by ownerId
      allow create: if isOwner(request.resource.data);
      allow update, delete: if isOwner(resource.data);
    }
    
    // Generic rule for all property subcollections
    match /properties/{propertyId}/{subcollection}/{docId}
    where subcollection in ['maintenanceLogs', 'inspections', 'documents', 'expenses', 'rentPayments'] {
      // For single document reads (get), check ownerId on the document itself.
      allow get: if isOwner(resource.data);
      // For collection group queries (list), check ownership of the parent property document.
      allow list: if get(/databases/$(database)/documents/properties/$(propertyId)).data.ownerId == request.auth.uid;
      
      // For writes, ensure the new/updated data has the correct ownerId.
      allow create: if isOwner(request.resource.data);
      allow update, delete: if isOwner(resource.data);
    }
  }
}
