rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // USER DATA
    // Grants a user access to their own user document and all documents
    // in all sub-collections (like tenants, screenings, etc.).
    // This is a secure and common pattern for user-owned data.
    match /users/{userId}/{path=**} {
      allow read, write: if request.auth.uid == userId;
    }

    // --- TOP-LEVEL COLLECTIONS ---

    // PROPERTIES
    match /properties/{propertyId} {
      // Only allow a user to get/update/delete their OWN properties.
      allow get, update, delete: if resource.data.ownerId == request.auth.uid;
      // Allow a user to create a property if the ownerId matches their UID.
      allow create: if request.resource.data.ownerId == request.auth.uid;
    }
    // Allow an authenticated user to LIST properties.
    // The client-side query MUST filter by ownerId to be secure, which the app already does.
    match /properties {
      allow list: if request.auth != null;
    }

    // CONTRACTORS
    match /contractors/{contractorId} {
      // Only allow a user to get/update/delete their OWN contractors.
      allow get, update, delete: if resource.data.ownerId == request.auth.uid;
      // Allow a user to create a contractor if the ownerId matches their UID.
      allow create: if request.resource.data.ownerId == request.auth.uid;
    }
    // Allow an authenticated user to LIST contractors.
    // The client-side query must filter by ownerId, which the app already does.
    match /contractors {
       allow list: if request.auth != null;
    }

    // --- PROPERTY SUB-COLLECTIONS ---
    // This single rule secures all sub-collections under any property.
    match /properties/{propertyId}/{subcollection}/{docId} {
      // To read or write to a property's subcollection, the user must own the parent property.
      allow read, write: if get(/databases/$(database)/documents/properties/$(propertyId)).data.ownerId == request.auth.uid;
    }
  }
}
