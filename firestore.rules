
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * RentSafeUK Security Governance v11.0
     * Optimized for high-speed query-compatible identity discovery and atomic handshakes.
     */

    function isSignedIn() {
      return request.auth != null;
    }

    function getEmail() {
      return request.auth.token.get('email', '').lower();
    }

    // Helper to check if the user is the landlord of the record
    function isLandlord(data) {
      return isSignedIn() && request.auth.uid == data.get('landlordId', '');
    }

    // Helper to check if the user is the tenant of the record (Case-Insensitive Handshake)
    function isTenant(data) {
      let email = getEmail();
      let uid = request.auth.uid;
      return isSignedIn() && (
        uid == data.get('tenantId', '') || 
        uid == data.get('userId', '') ||
        (email != "" && (
          email == data.get('email', '').lower() || 
          email == data.get('tenantEmail', '').lower()
        ))
      );
    }

    // --- Identity Registry ---
    match /users/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }

    // --- Tenant Registry ---
    match /tenants/{tenantId} {
      // Discovery: Authorized for query-based scans matching identity keys
      allow list: if isSignedIn();
      allow get: if isLandlord(resource.data) || isTenant(resource.data);
      allow create: if isSignedIn();
      
      // HANDSHAKE: Allow residents to link their UID if they match the registry email
      allow update: if isSignedIn() && (
        isLandlord(resource.data) || 
        (getEmail() == resource.data.get('email', '').lower() && request.resource.data.diff(resource.data).affectedKeys().hasAny(['userId', 'verified', 'joinedDate', 'status', 'lastSyncCheck']))
      );
      allow delete: if isLandlord(resource.data);
    }

    // --- Property Portfolio ---
    match /properties/{propertyId} {
      // Discovery: Authorized for query-based scans matching identity keys
      allow list: if isSignedIn();
      allow get: if isLandlord(resource.data) || isTenant(resource.data);
      allow create: if isSignedIn();
      
      // HANDSHAKE: Allow residents to link their UID to the property bridge
      allow update: if isSignedIn() && (
        isLandlord(resource.data) || 
        (getEmail() == resource.data.get('tenantEmail', '').lower() && request.resource.data.diff(resource.data).affectedKeys().hasAny(['tenantId', 'activeTenantUids', 'status']))
      );
      allow delete: if isLandlord(resource.data);
    }

    // --- Secure Messaging Ledger ---
    match /messages/{id} {
      // Optimized for high-speed query compatibility
      allow list: if isSignedIn() && (
        request.auth.uid == resource.data.get('tenantId', '') || 
        request.auth.uid == resource.data.get('landlordId', '') ||
        request.auth.uid == resource.data.get('senderId', '')
      );
      allow get: if isSignedIn() && (
        request.auth.uid == resource.data.get('tenantId', '') || 
        request.auth.uid == resource.data.get('landlordId', '') ||
        request.auth.uid == resource.data.get('senderId', '')
      );
      allow create: if isSignedIn() && (request.resource.data.get('senderId', '') == request.auth.uid);
      allow update, delete: if false;
    }

    // --- Shared Ledger Collections ---
    
    match /repairs/{id} {
      allow list: if isSignedIn() && (
        request.auth.uid == resource.data.get('landlordId', '') ||
        request.auth.uid == resource.data.get('tenantId', '') ||
        request.auth.uid == resource.data.get('reportedBy', '')
      );
      allow get: if isLandlord(resource.data) || isTenant(resource.data);
      allow create: if isSignedIn();
      allow update: if isLandlord(resource.data) || isTenant(resource.data);
      allow delete: if isLandlord(resource.data);
    }

    match /documents/{id} {
      allow list: if isSignedIn() && (
        request.auth.uid == resource.data.get('landlordId', '') ||
        request.auth.uid == resource.data.get('tenantId', '') ||
        request.auth.uid == resource.data.get('propertyId', '')
      );
      allow read: if isLandlord(resource.data) || isTenant(resource.data);
      allow write: if isLandlord(resource.data);
    }

    match /inspections/{id} {
      allow list: if isSignedIn() && (
        request.auth.uid == resource.data.get('landlordId', '') ||
        request.auth.uid == resource.data.get('tenantId', '')
      );
      allow read: if isLandlord(resource.data) || isTenant(resource.data);
      allow write: if isLandlord(resource.data);
    }

    match /rentPayments/{id} {
      allow list: if isSignedIn() && (
        request.auth.uid == resource.data.get('landlordId', '') ||
        request.auth.uid == resource.data.get('tenantId', '')
      );
      allow get: if isLandlord(resource.data) || isTenant(resource.data);
      allow write: if isLandlord(resource.data);
    }

    match /contractors/{id} {
      allow list: if isSignedIn();
      allow read, write: if isSignedIn();
    }

    match /expenses/{id} {
      allow list: if isSignedIn() && request.auth.uid == resource.data.get('landlordId', '');
      allow read, write: if isSignedIn() && request.auth.uid == resource.data.get('landlordId', '');
    }
    
    match /screenings/{id} {
      allow list: if isSignedIn() && request.auth.uid == resource.data.get('landlordId', '');
      allow read, write: if isSignedIn() && request.auth.uid == resource.data.get('landlordId', '');
    }

    match /checklists/{id} {
      allow list: if isSignedIn() && request.auth.uid == resource.data.get('landlordId', '');
      allow read, write: if isSignedIn() && request.auth.uid == resource.data.get('landlordId', '');
    }
  }
}
