
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Functions ---
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    // --- Collection Group Rules (Cross-Portfolio Discovery) ---
    
    /**
     * Discovery rules for Tenants and sub-collections.
     * Splitting conditions into separate blocks is a best-practice for 
     * authorizing collectionGroup list operations.
     */
    match /{path=**}/tenants/{tenantId} {
      // Allows residents to find their own identity records via email (Discovery)
      allow list, get: if isSignedIn() && resource.data.email == request.auth.token.email;
      // Allows landlords to list their own tenants via userId
      allow list, get: if isSignedIn() && resource.data.userId == request.auth.uid;
    }

    match /{path=**}/messages/{messageId} {
      allow list, get: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow list, get: if isSignedIn() && resource.data.senderId == request.auth.uid;
    }

    match /{path=**}/maintenanceLogs/{logId} {
      allow list, get: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow list, get: if isSignedIn() && resource.data.reportedBy == request.auth.uid;
      allow list, get: if isSignedIn() && resource.data.reportedBy == request.auth.token.email;
    }

    // --- Hierarchical Rules (Standard Portfolio Access) ---

    match /userProfiles/{userId} {
      allow get: if isOwner(userId);
      allow create: if isOwner(userId) && request.resource.data.id == userId;
      allow update: if isOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if false;

      /** 
       * Recursive portfolio oversight for Landlords.
       * Grants the property owner full read/write access to all nested data.
       */
      match /{allSubcollections=**} {
        allow read, write: if isOwner(userId);
      }
    }
  }
}
