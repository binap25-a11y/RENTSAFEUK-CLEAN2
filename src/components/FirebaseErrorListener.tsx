'use client';

/**
 * @fileOverview This component has been deprecated. 
 * Its logic has been integrated into FirebaseProvider to resolve circular dependency cycles 
 * that caused ChunkLoadErrors in the workstation environment.
 */
export function FirebaseErrorListener() {
  return null;
}
