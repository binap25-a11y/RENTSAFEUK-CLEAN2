'use client';

// This component's logic has been moved to src/firebase/provider.tsx to resolve a circular dependency issue.
// This file is no longer used and can be deleted in the future.

export function FirebaseErrorListener() {
  return null;
}
