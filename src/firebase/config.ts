
// 1. Go to your Firebase project console: https://console.firebase.google.com/
// 2. In the left menu, click Project Settings (the gear icon).
// 3. Scroll down to the "Your apps" section.
// 4. If you don't already have a Web app, click the Web icon (</>) to create one.
// 5. After creating the app, you will find the `firebaseConfig` object in the "SDK setup and configuration" section.
// 6. Copy the entire object and paste it below, replacing the placeholder object.

export const firebaseConfig = {
  apiKey: "REPLACE_WITH_YOUR_API_KEY",
    authDomain: "REPLACE_WITH_YOUR_AUTH_DOMAIN",
      projectId: "REPLACE_WITH_YOUR_PROJECT_ID",
        storageBucket: "REPLACE_WITH_YOUR_STORAGE_BUCKET",
          messagingSenderId: "REPLACE_WITH_YOUR_MESSAGING_SENDER_ID",
            appId: "REPLACE_WITH_YOUR_APP_ID"
            };
            